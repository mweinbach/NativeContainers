# Feature matrix

This is the product contract for “OrbStack-class,” with exact public-API gaps
called out rather than papered over.

| Capability | Native implementation | Phase | Constraint |
| --- | --- | --- | --- |
| OCI pull/push/list/tag/prune | Apple image services | M1 | Reviewed exact-platform pull/push, rich inspect, safe tag/delete, and prune are live; public push smoke is intentionally prohibited |
| Dockerfile/Containerfile builds | Planning/execution/lifecycle services + Apple `ContainerBuild` | M2 | Reviewed exact-platform builds are live through a signed worker; the facade is independently injectable by phase, while 1.0.0 still requires Dockerfiles below 16 KiB and lacks structured cache-only prune |
| Build output publication | Reviewed host destination service + private artifact stores | M2 | OCI, root-filesystem tar, and flat local-folder output are live-verified; user destinations never enter the worker protocol, replacement is explicit and exact, publication is cancellation-aware and atomic, and root-filesystem outputs are deliberately single-platform |
| Build secrets | Reviewed file-descriptor vault + Apple BuildKit secret payload | M2 | Private files outside the context stream once over worker stdin; plans and Codable control frames contain no bytes, and secret-build diagnostics are suppressed |
| Build history | App-owned recording decorator + private file store | M2 | Typed running/terminal outcomes and output kinds, live-launch-aware crash reconciliation, token-gated cross-process refresh, coalesced slow-I/O updates, corruption/schema isolation, fail-closed bounded scans, and 200-record terminal retention are live; destinations, paths, logs, error text, secret metadata, and option values are not persisted |
| Build cache profiles | Typed protocol-v5 mode + transactional app-owned local-cache service | M2 | Shared builder cache, no-cache, and one fixed local profile are available; raw BuildKit strings and user paths never cross the worker boundary, a token/full-tree-fingerprint-bound prepared handoff survives intervening inspection, stale handoffs are bounded to 24 hours, promotion follows artifact isolation, cancellation/hard-exit recovery preserves the prior generation, reset touches only the app namespace, and the two-build export/import path is live-verified; independent hit attribution after deleting Apple's internal builder cache remains intentionally unclaimed |
| Shared builder/cache maintenance | Container service XPC + exact snapshot adapter | M2 | Stable status, whole-bundle allocation, reviewed Stop/Force Stop, and stopped-only reset are live; external CLI activity is not observable |
| Storage accounting | Focused Apple XPC adapter + descriptor-relative VM library scanner + app-scoped observable model | M4 | Explicit on-demand measurement reports Apple image/container/volume allocation and point-in-time reclaimability plus VM provisioned/logical/allocated/saved-state bytes; scans include hidden partials, deduplicate hard links, never follow links or cross mounts, propagate Cancel and view-disappearance cancellation, and never run during ordinary inventory refresh. APFS shared extents remain filesystem-reported estimates, not uniquely attributable reclaimable bytes. |
| Apple runtime storage reclamation | Reviewed container/image/volume plans + bounded XPC adapters + app-scoped review model | M4 | Exact candidates, accounting/inventory provenance, destructive confirmation, commit-time identity checks, deterministic container→image→volume execution, cancellation checkpoints, and partial-result reconciliation are live. Stopped-container cleanup is opt-in and only accepts UUID-owned app containers; it never auto-stops, KILLs, force-deletes, touches Compose/builder/machine/Apple-managed resources, expands its plan, or mutates VM storage. |
| VM storage reclamation | Saved-state store + descriptor-safe artifact inspector + lease-aware category services + app-scoped review model | M4 | Reviewed committed saved states, exact app-owned interrupted-operation residue, and opt-in unreferenced restore images are live with accounting/library provenance, operation/runtime/cache locks, device/inode/tree seals, link/special-file/mount rejection, atomic retirement, and partial results. Restore-image execution reloads VM references, preserves active/referenced files, requires seven-day age for partials, and never replaces or expands the reviewed plan. The path never starts, stops, force-stops, or kills a VM and never touches VM disks. |
| Container lifecycle | `ContainerClient` | M1 | Foundation start/stop/delete is wired |
| Unified resource navigation | Typed workspace routes + inventory-derived search catalog | M1 | Command-K Quick Open and Overview links target exact resources in one main window; authoritative deletion falls back safely, transient failures preserve the pending route, localized kind names are searchable, and active build plans retain their navigation lock |
| Menu-bar quick controls | SwiftUI `MenuBarExtra` + shared `AppModel` lifecycle paths | M2 | The optional persistent control shows authoritative runtime/resource counts and up to eight identity-stable containers. Start, graceful Stop, Restart, and explicit Force Stop reuse the main window's services and refresh/error reconciliation; Force Stop remains reachable during a hung stopping state, and no second inventory poller or lifecycle implementation exists. |
| Launch at login | `SMAppService.mainApp` + injectable launch service/model | M5 | Registration is idempotent and reports disabled, enabled, approval-required, or unavailable state. Debug/non-installable copies fail visibly; NativeContainers does not write a launch-agent plist or claim approval before System Settings grants it. |
| Exec, logs, copy, inspect, stats | `ContainerClient` + SwiftTerm | M1 | Non-interactive exec and native interactive PTY are live |
| Volumes and named networks | Apple services | M1 | Reviewed create/delete/prune, capacity versus allocated usage, built-in protection, and configured-container use checks are live; Apple 1.0 has no conditional delete token |
| Container storage/network/socket attachments | Focused attachment service + Apple configuration types | M1 | Exact reviewed named-volume identities, ordered networks, and private operation-scoped Unix sockets are live and revalidated before mutation/start |
| Container-to-host alias | Read-only resolver/PF discovery | Partial | Exact configured-on-disk state can be selected and a fixed privileged command is shown; the GUI does not mutate or claim active PF state, and a signed helper remains future work |
| Direct container IP and published ports | Apple vmnet/socket forwarders | M1 | Dedicated IPs and TCP/UDP ranges are inventoried; TCP host publications offer explicit revalidated HTTP/HTTPS opening; no exact shared host loopback |
| Docker CLI and Engine API | Pinned Socktainer install/process/context services | M2 | Settings-managed verified install, Apple API health, HTTP readiness, exact-PID TERM-to-KILL and Force Stop, crash-residue recovery, and non-activating `nativecontainers` context are live; Engine coverage remains partial API v1.51 |
| Docker Compose | Review/planning/revalidation/journal/mutation services + private official client + optional Docker CLI bridge | M2 | Docker Compose 5.1.4 is version/digest/provenance pinned. Fresh and contiguous-prefix create-missing Up use a stable immutable config path, a hash-proven external-resource overlay, native managed-resource creation, and exact attachment postconditions. Exact-ID Start/Stop and reviewed declared-container/orphan/network/named-volume Down remain identity sealed; cancellation leaves a manual-only durable recovery record. Recreation, aliases, health checks, restart policies, configs, secrets, and unallowlisted canonical keys remain blocked. Apple 1.0 mutation APIs have no conditional identity token, so an external client can still race the final revalidation-to-mutation gap. |
| Registry credentials | Apple Keychain client | M1 | Login/list/logout live; stored secrets never leave Keychain |
| Rosetta `linux/amd64` applications | Apple Containerization | M1 | ARM Linux guest; not an x86 VM |
| Persistent Linux dev machines | Bounded machine/process XPC transports + focused lifecycle, process-target, command, terminal, and inventory services | M3 | Native create, first-boot provisioning, start, graceful stop, identity-pinned KILL, stopped-only delete, CPU/memory, reviewed home mounts, mapped-user shell commands, and interactive PTY are live; Apple 1.0 deletion is ID-only, configuration editing remains future work, and boot forwards host `SSH_AUTH_SOCK` when present even with no home mount |
| Shared-kernel/project density | Experimental `LinuxPod` | M5 | Opt-in only after upstream stabilization |
| Kubernetes | k3s in a dedicated Linux machine | M5 | Separate lifecycle and storage plan |
| macOS restore/install | Focused acquisition, durable-store maintenance, manifest-reference, bundle-resolution, staged-media, and `VZMacOSInstaller` services | M4 | New download/import/preparation leases live in backup-excluded Application Support and span manifest commit. Launch recovery migrates exact referenced legacy Caches IPSWs with dual-store/library locks, copy-first phase journals, immutable UUID targets, and idempotent per-manifest replacement while retaining the old copy. Install UI, progress, supported cancellation, retry-safe staging, interruption recovery, and deterministic tests are complete; successful installation releases the manifest reference. A real install still awaits the Virtualization entitlement and local-IPSW smoke gate. |
| macOS display/input | Generation-keyed `VZVirtualMachineView` / `VZVirtualMachineViewAdaptor` bridge | M4 | Native console, automatic display reconfiguration, explicit system-key capture, and stale-view detachment are implemented; live use awaits the entitlement gate |
| VM pause/resume/stop/recovery | App-scoped runtime coordinator + per-VM advisory lease + `VZVirtualMachine` delegate | M4 | Start/pause/resume/graceful stop/destructive stop are state-validated; terminal events release ownership exactly once, caller cancellation never abandons an accepted start, and a generation-pinned 30-second watchdog force-stops a hung graceful shutdown |
| VM shared folders | Modular bookmark, sidecar, lease-orchestration, and VirtioFS services | M4 | Persistent read-only/read-write shares, private capability storage, stopped-only inspector editing, saved-state fingerprinting, and one macOS automount device are implemented; live guest mount verification awaits the entitlement and an installed guest |
| VM audio | Virtio sound | M4 | Microphone privacy configuration needed |
| Same-host suspend/resume | Modular runtime/saved-state services + VZ save/restore | M4 | Transactional, configuration-bound, lease-pinned, single-use restore and explicit Start Fresh/Discard are implemented with deterministic tests; saved state is not portable and live verification awaits the Virtualization entitlement |
| Same-host VM cloning | Transaction store + cancellable `copyfile` clone/sparse transfer + fresh VZ platform identity | M4 | Stopped-only clones are atomically published, strip nonportable saved/runtime state, reject links and stale ownership, stop fallback copies through write-level callbacks, clean partials on cancellation/restart, and cold boot with a distinct `VZMacMachineIdentifier`; live guest boot still awaits the Virtualization entitlement |
| Portable VM backup/import | Shared bundle-preparation service + export source lease + import transaction store | M4 | `.nativevm` packages preserve identity for restore or explicitly generate fresh manifest/platform identities for copy import; transfers reject links and special files, strip saved/runtime/cache/bookmark state, never replace an export, expose cancellation through copyfile, clean import partials after cancellation/restart, and publish atomically |
| VM sparse disk migration | DiskImageKit geometry/attachment adapter + stopped-only migration coordinator + exact-PID `diskutil` converter | M4 | macOS 27 RAW-to-ASIF conversion is out-of-place, saved-state-free, identity-sealed, phase-journaled, cancellation-safe, and launch-recoverable. Unconfirmed process exit is quarantined; failed KILL delivery requires a host restart, and runtime/transfer/discard paths reject pending migration artifacts. The manifest flips only after ASIF format and virtual capacity validation; cleanup then retires the old RAW disk without another cancellation point. No raw truncation or in-place resize is used. |
| Efficient disk snapshots | DiskImageKit overlays | M5 | macOS 27; shallow layer stacks |
| Physical USB passthrough | AccessoryAccess + VZ USB | M5 | macOS 27 and user-granted entitlement |
| Linux GPU/Metal passthrough | None | Unavailable | No public Apple API |
| Intel macOS guests on Apple silicon | None | Unavailable | Unsupported architecture |
| Full guest-memory reclamation | Virtio balloon | Partial | Cooperative, not guaranteed |

## Performance gates

Every implementation milestone should record:

- cold and warm container startup;
- 1, 10, and 50 idle-container resident memory;
- memory retained after guest stress and idle-stop;
- bind-mount metadata and sequential I/O;
- PostgreSQL durability/fsync behavior;
- image pull/build time and allocated disk growth;
- NAT/direct-IP latency and throughput;
- sleep/wake and crash-recovery behavior.

Optimizations belong behind measured regressions. The Apple runtime’s existing
optimized kernel, sparse ext4 images, APFS clone-on-write roots, dedicated IPs,
and launchd-managed helpers are the starting point, not code to replace.
